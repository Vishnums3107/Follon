# Adapters

This directory will contain broker, market-data, and notification integrations. Adapters translate external formats to canonical contracts and must not own risk policy, portfolio truth, or domain-state transitions.
