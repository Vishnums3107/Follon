{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop window with bounded native trading commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
