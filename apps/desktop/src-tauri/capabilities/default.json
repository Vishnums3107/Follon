{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Read-only desktop window with no custom native commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
