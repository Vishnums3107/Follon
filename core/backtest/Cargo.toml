[package]
name = "follon-backtest"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
follon-control-plane = { path = "../control-plane" }
follon-domain = { path = "../domain" }
follon-market-data = { path = "../market-data" }
serde_json = "1.0.145"
sha2 = "0.10.9"

[dev-dependencies]
follon-instrument = { path = "../instrument" }

[lib]
path = "src/lib.rs"

[lints]
workspace = true
